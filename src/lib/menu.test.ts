import {expect, describe, it} from 'vitest'

describe('Menu', () => {
  describe('placeOrder', () => {
    it.skip('returns the items ordered', () => {
      const menu = {
        // Main Side Drink
        // 1: Eggs 2: Toast 3: Coffee
        items: ["Eggs", "Toast", "Coffee"],
        ordered: [1,2,3],
      }
    })
  })
})
